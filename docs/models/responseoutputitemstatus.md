# ResponseOutputItemStatus

Output status

## Example Usage

```typescript
import { ResponseOutputItemStatus } from "@r9s/sdk/models";

let value: ResponseOutputItemStatus = "completed";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"completed" | "in_progress" | "incomplete" | Unrecognized<string>
```