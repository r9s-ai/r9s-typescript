# SearchResponseEngine

Search engine used

## Example Usage

```typescript
import { SearchResponseEngine } from "@r9s/sdk/models";

let value: SearchResponseEngine = "tavily";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"exa" | "tavily" | "google" | Unrecognized<string>
```