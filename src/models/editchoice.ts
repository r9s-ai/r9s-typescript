/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";

export type EditChoice = {
  text: string;
  index: number;
};

/** @internal */
export const EditChoice$inboundSchema: z.ZodMiniType<EditChoice, unknown> = z
  .object({
    text: types.string(),
    index: types.number(),
  });

export function editChoiceFromJSON(
  jsonString: string,
): SafeParseResult<EditChoice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditChoice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditChoice' from JSON`,
  );
}
